/** @type {import('tailwindcss').Config} */
export const theme = {
    extend: {
        colors: {
            brand: {
                DEFAULT: '#FF6C0C', // brand
                light: '#6366F1',
                dark: '#4338CA',
            }
        }
    }
};
